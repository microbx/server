
mutation CreateLink {
    createLink (
        url: "http://npmjs.com/package/graphql-tools",
        description: "Best Tools!"
    ) {
        id
        url
        description
        postedBy {
            name
        }
    }
}

mutation CreateLink {
    createLink (
        url: "https://jwt.io/",
        description: "Better token generation method"
    ) {
        id
        postedBy {
            name
        }
    }
}

mutation CreateLink {
  createLink(
    url:"www.microbe3.com"
    description: "Greatest Site 3"
    postedById: "59cd3282814f3c1c55b136e4"
  ) {
    id
    dateCreated
    url
    description
  }
}
